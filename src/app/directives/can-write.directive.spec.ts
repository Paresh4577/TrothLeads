import { CanWriteDirective } from './can-write.directive';

describe('CanWriteDirective', () => {
  it('should create an instance', () => {
    const directive = new CanWriteDirective();
    expect(directive).toBeTruthy();
  });
});
